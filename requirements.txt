ruff==0.14.5
    # Linter и автоматический форматтер Python кода, помогает соблюдать PEP8 и другие правила качества кода
python-dotenv==1.2.1
    # Загрузка переменных окружения из .env файлов, удобна для конфигурации приложений
requests==2.32.5
    # Библиотека для HTTP-запросов, широко используется для взаимодействия с веб-сервисами
gigachat==0.1.43
    # Клиентская библиотека для интеграции с GigaChat, позволяет отправлять и получать сообщения через API
lightrag-hku==1.4.9.8
    # Набор утилит и инструментов для работы с AI-моделями и хранилищами данных
nano-vectordb==0.0.4.3
    # Минималистичная библиотека для работы с векторными базами данных, поддержка поиска по embedding
networkx==3.5
    # Библиотека для создания, анализа и визуализации графов и сетей
numpy==2.3.4
    # Библиотека для работы с массивами и матрицами, поддержка высокопроизводительных математических операций
